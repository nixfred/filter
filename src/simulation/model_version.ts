// Simulation model version, independent of the application release (ruling R015, OPS009).
// Bump when any change alters simulation behavior for the same seed and parameters,
// and regenerate the determinism fixtures in the same commit (docs/TEST_PLAN.md 3.4).
export const SIMULATION_MODEL_VERSION = 1;
